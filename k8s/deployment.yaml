apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-##DOCKER_USERNAME##
  namespace: ##K8S_NAMESPACE##
  labels:
    app: app-##DOCKER_USERNAME##
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app: app-##DOCKER_USERNAME##
  template:
    metadata:
      labels:
        app: app-##DOCKER_USERNAME##
    spec:
      containers:
      - name: app-##DOCKER_USERNAME##
        image: docker.io/##DOCKER_USERNAME##/satc-devops:##K8S_TAG##
        ports:
        - containerPort: 4278
          name: http
---
apiVersion: v1
kind: Service
metadata:
  name: app-##DOCKER_USERNAME##
  namespace: ##K8S_NAMESPACE##
  labels:
    app: app-##DOCKER_USERNAME##
spec:
  type: ClusterIP
  selector:
    app: app-##DOCKER_USERNAME##
  ports:
  - port: 80
    targetPort: 4278
    name: http
    protocol: TCP
---
# ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: app-##DOCKER_USERNAME##
  namespace: ##K8S_NAMESPACE##
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  ingressClassName: nginx
  rules:
  - host: ##DOCKER_USERNAME##.app.devops-satc.online
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: app-##DOCKER_USERNAME##
            port:
              number: 80